# Test Environment Configuration
NODE_ENV=test

# Redis Configuration (same as dev for integration tests)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Test Database Configuration
DATABASE_URL=postgresql://tilemud_user:tilemud_pass@localhost:5433/tilemud_test
DB_HOST=localhost
DB_PORT=5433
DB_NAME=tilemud_test
DB_USER=tilemud_user
DB_PASSWORD=tilemud_pass

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Session Configuration (faster for tests)
SESSION_SECRET=test-session-secret
GRACE_PERIOD_SECONDS=5
MAX_ACTIVE_SESSIONS=10
SESSION_TIMEOUT_SECONDS=3600
RECONNECTION_TOKEN_TTL=10
HEARTBEAT_INTERVAL_SECONDS=5

# WebSocket Configuration
WS_PORT=3336

# Logging
LOG_LEVEL=warn