import { describe, expect, it } from "vitest";

describe("Integration: capacity and auth failures", () => {
  it("denies session when capacity exhausted or token invalid with correct UX messaging", async () => {
    expect.fail("Not implemented: requires capacity guard + auth failure handling");
  });
});
