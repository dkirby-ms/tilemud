import { describe, expect, it } from "vitest";

describe("Integration: mid-session version deprecation", () => {
  it("disconnects active clients when server version increments mid-session", async () => {
    expect.fail("Not implemented: requires version guard broadcast");
  });
});
