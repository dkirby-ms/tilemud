import { describe, expect, it } from "vitest";

describe("Instance tile broadcast", () => {
  it("broadcasts tile placements to all participants", async () => {
    expect.fail("Not implemented: requires simulated room runtime");
  });

  it("records monotonic timestamps for latency assertions", async () => {
    expect.fail("Not implemented: requires simulated room runtime");
  });
});
