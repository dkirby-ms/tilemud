import { describe, expect, it } from "vitest";

describe("Integration: restart recovery", () => {
  it("restores acknowledged actions after server restart", async () => {
    expect.fail("Not implemented: requires durable replay + restart harness");
  });
});
