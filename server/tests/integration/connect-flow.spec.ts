import { describe, expect, it } from "vitest";

describe("Integration: connect flow", () => {
  it("drives a player session from CONNECTING to ACTIVE with initial snapshot", async () => {
    expect.fail("Not implemented: requires session bootstrap + real-time handshake integration");
  });
});
