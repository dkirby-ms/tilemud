import { describe, expect, it } from "vitest";

describe("Instance NPC ordering", () => {
  it("executes scripted events according to priority tiers", async () => {
    expect.fail("Not implemented: requires event scheduler");
  });

  it("maintains deterministic ordering with player actions", async () => {
    expect.fail("Not implemented: requires event scheduler");
  });
});
