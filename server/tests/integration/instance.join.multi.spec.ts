import { describe, expect, it } from "vitest";

describe("Instance join - multiplayer", () => {
  it("synchronizes initial state across multiple players", async () => {
    expect.fail("Not implemented: awaits multiplayer integration harness");
  });

  it("broadcasts participant list upon join", async () => {
    expect.fail("Not implemented: awaits multiplayer integration harness");
  });
});
