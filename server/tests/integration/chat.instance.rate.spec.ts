import { describe, expect, it } from "vitest";

describe("Instance chat rate limiting", () => {
  it("delivers messages under the per-player threshold", async () => {
    expect.fail("Not implemented: requires chat service integration");
  });

  it("rejects messages when the threshold is exceeded", async () => {
    expect.fail("Not implemented: requires chat service integration");
  });
});
