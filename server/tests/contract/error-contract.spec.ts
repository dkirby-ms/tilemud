import { describe, expect, it } from "vitest";

describe("Contract: error response shape", () => {
  it("returns canonical error contract with code, category, retryable, correlation_id", async () => {
    expect.fail("Not implemented: requires error catalog integration");
  });
});
