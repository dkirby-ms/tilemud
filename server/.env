## Local development example environment
## Copy to `.env` (or export manually) and adjust as needed.
## These values are aligned with infrastructure defaults produced by `./infrastructure/scripts/infra-up.sh`.

DATABASE_URL=postgres://tilemud:tilemud_dev_pw@localhost:5438/tilemud
REDIS_URL=redis://localhost:6380/0
PORT=4000
LOG_LEVEL=info
