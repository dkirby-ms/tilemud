import { describe, expect, it } from "vitest";

describe("Client diagnostics overlay", () => {
  it("shows latency and reconnect indicators in development mode", () => {
    expect.fail("Not implemented: requires diagnostics overlay implementation");
  });
});
