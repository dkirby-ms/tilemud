import { describe, expect, it } from "vitest";

describe("State delta reducer", () => {
  it("applies event.state_delta payloads to client state snapshots", () => {
    expect.fail("Not implemented: requires state reducer logic");
  });
});
