import { describe, expect, it } from "vitest";

describe("Integration: version mismatch UX", () => {
  it("disconnects the client and surfaces update-required messaging when server version differs", async () => {
    expect.fail("Not implemented: requires client-server version guard wiring");
  });
});
