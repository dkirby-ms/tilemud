import { describe } from "vitest";

// Placeholder kept temporarily so editors that cached the previous extension
// don't crash when trying to open the removed file. All logic now resides in
// `version-mismatch.spec.ts`.
describe.skip("version mismatch spec placeholder", () => {
	// intentionally empty
});
