import { describe, expect, it } from "vitest";

describe("Integration: reconnect UI", () => {
  it("shows reconnecting overlay and transitions back to active session after recovery", async () => {
    expect.fail("Not implemented: requires reconnect UI state wiring");
  });
});
